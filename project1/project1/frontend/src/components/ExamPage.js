import React from 'react';
import ProcturedExam from './ProcturedExam';

// This component serves as a standalone page for the proctured exam
// It will be accessed via a direct URL in a new tab
export default function ExamPage() {
  return <ProcturedExam />;
}
